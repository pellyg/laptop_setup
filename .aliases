
alias touchall='git diff origin/master --name-only | xargs touch'

DefaultsDeveloperToolsEnabled -bool YES'


stashgrep() {
  IFS=$'\n'
  for i in `git stash list --format="%gd"`; do
    git stash show -p $i | grep -H --label="$i" "$1"
  done
}
